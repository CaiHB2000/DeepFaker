This folder holds figures for the ARR submission.

Placeholders are used by default so you can compile and inspect layout on Overleaf without actual images yet. Replace these with real PDFs/PNGs when results are ready:

- reliability_main.pdf         # Reliability diagram (ECE) for main result
- gating_coverage.pdf          # Gate selection ratio vs. epoch/threshold
- event_reliability_hist.pdf   # Histogram of event reliability scores
- necessity_overview.pdf       # 3-panel motivation: event reliability, overconfident teacher errors, modality disagreement vs. error
 - framework_overview.pdf       # Overall method diagram (you will replace with the final artwork)
- per_event_bars.pdf           # Per-event Macro-F1/Pos-F1 bars (optional)

If you don't have the images yet, the paper will render gray boxes as placeholders so page layout is visible.
