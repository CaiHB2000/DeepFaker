# 2025-10-29 实验进展汇总（动态蒸馏项目）

## 已完成工作

- **Twitter 多随机种子训练（动态蒸馏）**
  - 成功跑通 seeds {0,1,2,3}（均使用 `ddistill` 环境、单卡 GPU 5）。
  - 结果存放：`paper_results/twitter/…`，汇总表：`paper_results/tables/twitter_dynamic_full_multi_seed.csv`，概览图：`paper_results/figures/twitter_dynamic_full_seed_f1.png`。
  - 核心指标（宏平均）：
    - Accuracy：0.99755 ± 0.00050
    - Macro-F1：0.99755 ± 0.00050
    - Positive-F1：0.99747 ± 0.00052
    - ECE：0.00244 ± 0.00050
  - Seed=3 表现最佳（Acc/F1≈0.9982，ECE≈0.00183）。

- **Weibo 已有结果整合**
  - 延续昨日（10-28）记录：`paper_results/tables/weibo_multi_seed_results.csv`，均值 Accuracy≈0.9215、ECE≈0.0577。

- **数据资源准备（图结构实验）**
  - 下载 Fakeddit 评论全集（`datasets/fakeddit/comments/all_comments.tsv`，含 `submission_id`、`parent_id`、`isTopLevel` 等，支持构建 Reddit 评论传播树）。
  - 克隆 WeFEND（WeChat）数据集：`datasets/wechat/`，可利用 `Report Content` 作为中文传播反馈。
  - 整理文献 MUGCL（s1）关于图构建方法，为后续图蒸馏做方案设计。

- **脚本修复/产物追加**
  - 修正 `run_experiments.py` 的 run_name 重复附加问题（防止 summary 路径缺失）。
  - 生成 Twitter 结果汇总表 & F1 条形图，便于快速对比。

## 当前问题

- **Seed=4 训练未完成**：两次运行因时长超过 CLI 默认超时被终止，目录 `twitter_20251029-053102_seed04` / `…073135_seed04` 仅含模型 ckpt，无指标文件。
- **缺少 Twitter 基线**：尚未在 Twitter 上跑 `γ=0`（不蒸馏）等对照实验，暂无法直接与我们方法比较。
- **OOD（跨域）尚未评估**：目前只在各自训练/测试集上汇报，需要补充“训练 Weibo、测试 Twitter”等跨域验证。
- **图蒸馏尚未启动实现**：虽已备好 Fakeddit 评论数据，但还未编写图构建 & 模型对接脚本。

## 下一步计划

1. **完成 Twitter 多种子**
   - 重新启动 seed=4（延长 CLI 超时或拆分成后台进程）；
   - 补充 `no_distill`、`high_delta` 等基线，生成对照表。
2. **OOD 泛化实验**
   - 设计 cross-domain 评测流程：如在 Weibo 训练的最佳权重直接评估 Twitter（及反向）；必要时统一文本 tokenizer。
   - 产出 OOD 指标（Acc/F1/ECE）并绘制对比图。
3. **图网络接入**
   - Fakeddit：编写 `tools/build_fakeddit_graphs.py`，将 `all_comments.tsv` -> 节点/边 JSON；构建 text/image-rooted 图。
   - WeChat：解析 `Report Content`，设计简化传播图；若需要评论原文，调研追加数据源。
   - 实现 MUGCL 风格的图蒸馏模块（节点级 + 图级蒸馏，支持不确定性指导）。
4. **实验文档与可视化**
   - 将 Twitter / Weibo / OOD / 图网络实验统一写入 `paper_results/tables/` 与 `paper_results/figures/`，生成对比表与折线/柱状图。
   - 更新 docs 中的技术报告，确保论文撰写所需的全部指标与图表齐备。

> 提醒：继续所有训练时请使用 `conda activate ddistill`，并确认日志目录写入 `paper_results/…`，以便次日汇总。 Seed=4 与基线实验完成后需更新本报告。
