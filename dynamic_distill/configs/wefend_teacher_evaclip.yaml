model:
  num_classes: 2
  encoder_dim: 1024
  classifier_hidden: 512
  dropout: 0.1
  text_model: models/deberta-v3-large
  vision_model: models/vit-huge-patch14-224

optim:
  lr: 2.0e-05
  weight_decay: 0.01

loss:
  alpha: 0.5
  beta: 0.3
  gamma: 0.0
  evidence_anneal_steps: 1500

training:
  epochs: 12
  batch_size: 8
  log_every: 50
  max_steps_per_epoch: null
  allow_data_parallel: true

scheduler:
  type: cosine
  warmup_steps: 800

teacher:
  use_ema: true
  ema_decay: 0.998

logging:
  output_dir: paper_results/wefend_teacher_evaclip
  run_name: wefend_teacher_evaclip
  overwrite: true

tokenizer:
  name: models/deberta-v3-large
  max_length: 160
  local_files_only: true

vision_processor:
  name: models/vit-huge-patch14-224
  image_size: 224
  local_files_only: true
  augment:
    enabled: true
    flip_prob: 0.5
    color_jitter: 0.1
    random_rescale: 0.1

data:
  type: wefend
  root: datasets/wechat
  num_workers: 8
  pin_memory: true
  train:
    csv_file: processed_split/wefend_train.csv
    image_dir: images
  val:
    csv_file: processed_split/wefend_val.csv
    image_dir: images
  test:
    csv_file: processed_split/wefend_test.csv
    image_dir: images
